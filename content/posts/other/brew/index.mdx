---
slug: other-brew
title: brew安装
author: 袁官东
date: 2021-08-04
hero: ./hero.webp
excerpt: brew 是 MacOS 上的包管理工具，可以简化 macOS 和 Linux 操作系统上软件的安装。
---

<div className={"img-desc"}>图：Mako Tsereteli</div>

brew 是 MacOS 上的包管理工具，可以简化 macOS 和 Linux 操作系统上软件的安装。

## 确认安装 ruby

brew 是 ruby 开发的，需要确认 ruby 是否已安装，默认是已经安装的。

```bash
$ which ruby
$ ruby --version
```

## 安装
执行命令:
```bash
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
```
目前此命令可用，按提示选择下载源，我选的清华下载源
```bash
/bin/zsh -c "$(curl -fsSL https://gitee.com/cunkai/HomebrewCN/raw/master/Homebrew.sh)"
```

## 确认
```bash
brew --version
```
测试是否已安装成功
```bash
which brew
```
安装成功则显示路径